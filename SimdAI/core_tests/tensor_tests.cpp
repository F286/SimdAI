#include <catch2/catch.hpp>
#include "core/tensor.h"

TEST_CASE("Tensor multiplication is computed", "[multiply]") {
    Tensor<float> a{1, 2}; // Simple 2x8 and 8x8 matrices for example
    // Initialize tensors 'a' and 'b' with some values
    // ...

    //Tensor<float> c = multiply(a, b);

    // Write assertions to verify the correctness of c
    // ...
}
